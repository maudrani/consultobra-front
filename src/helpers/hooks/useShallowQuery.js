import React from 'react';
import { useRouter } from 'next/router';

const useShallowQuery = (props) => {
    const { initialValues, newValues } = props

    const Router = useRouter()


}

export default useShallowQuery;