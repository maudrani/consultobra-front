import { styled } from '@material-ui/core'
import InfoArea from 'src/components/material-edited/InfoArea/InfoArea'

export const InfoAreaStyled = styled(InfoArea)(() => ({
    padding: '40px 0 40px',
}))
